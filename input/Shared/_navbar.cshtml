@model string
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="@Site.Program.Config.BaseHttpAddress/index.html">MRKC</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
	
    <ul class="nav navbar-nav">
 	  @{ if (Model == "service" ) {<text><li class="active"><a href="@Site.Program.Config.BaseHttpAddress/service">Services<span class="sr-only">(current)</span></a></li></text>}else{<text><li><a href="@Site.Program.Config.BaseHttpAddress/service">Services</a></li></text>}}
	  @{ if (Model == "blog" ) {<text><li class="active"><a href="@Site.Program.Config.BaseHttpAddress/blog">Blog<span class="sr-only">(current)</span></a></li></text>}else{<text><li><a href="@Site.Program.Config.BaseHttpAddress/blog">Services</a></li></text>}}
	  @{ if (Model == "about" ) {<text><li class="active"><a href="@Site.Program.Config.BaseHttpAddress/about">About<span class="sr-only">(current)</span></a></li></text>}else{<text><li><a href="@Site.Program.Config.BaseHttpAddress/about">About</a></li></text>}}
	  @{ if (Model == "contact" ) {<text><li class="active"><a href="@Site.Program.Config.BaseHttpAddress/index.html">Contact<span class="sr-only">(current)</span></a></li></text>}else{<text><li><a href="@Site.Program.Config.BaseHttpAddress/contact">Contact</a></li></text>}}
 
	  @{ if (Model == "blog")
    {<text>
      <script>
      function searchBlog() {
        alert('Sorry, still working on the search functionality.');
        return false;
      }
      </script>
        <ul class="nav navbar-nav navbar-right">
        <form class="navbar-form navbar-right" onsubmit="return searchBlog();">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search Blog">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
        </form>
        </ul>
      }
      </text>
	  }
    }
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>